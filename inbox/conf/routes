# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# For dev machines
GET     /assets/*path               dev.DevAssetsController.at(path)

GET     /_healthcheck               conf.Healthcheck.healthcheck()

GET     /inbox/:userId                  controllers.InboxController.getPosts(userId)
POST    /inbox/:userId/:addedAt/read    controllers.InboxController.setRead(userId, addedAt: Long)
